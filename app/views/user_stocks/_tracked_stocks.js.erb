$("#stocks-table-body").append("<%= j render partial: 'tracked_stocks.html', locals: { stock: @stock } %>")
if ($("table").length === 0){
    <% @stocks = [@stock]%>
    $("body").append("<%= j render partial: "table_tracked_stocks"%>")
}
$('.alert').remove()
$('#result').prepend("<%= j render partial: 'layouts/messages'%>")
